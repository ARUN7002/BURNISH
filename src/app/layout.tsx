import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BURNISH — AI Repository Governance Platform",
  description:
    "Enterprise-grade AI-powered GitHub repository governance. Get governance scores, risk analysis, deployment readiness, and executive reports in minutes.",
  keywords: [
    "BURNISH",
    "repository governance",
    "code quality",
    "AI analysis",
    "GitHub",
    "software governance",
    "risk assessment",
    "deployment readiness",
  ],
  authors: [{ name: "BURNISH Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
